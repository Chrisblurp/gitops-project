apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-app
  labels:
    app: gitops-app
    version: blue                    
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gitops-app
      version: blue                  
  template:
    metadata:
      labels:
        app: gitops-app
        version: blue                
    spec:
      containers:
      - name: gitops-app
        image: khrisblurp/gitops-project-app:1
        ports:
        - containerPort: 5050
        env:
        - name: APP_VERSION
          value: "blue"
        livenessProbe:
          httpGet:
            path: /health
            port: 5050
          initialDelaySeconds: 5
          periodSeconds: 10